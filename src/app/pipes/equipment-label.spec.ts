import { EquipmentLabel } from './equipment-label';

describe('EquipmentLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentLabel();
    expect(pipe).toBeTruthy();
  });
});
